option(CLI_OVPNDCOWIN "Build ovpncli with ovpn-dco-win driver support" OFF)
if (APPLE)

  add_executable(iitdvpncli openvpn.cpp)
  add_core_dependencies(iitdvpncli)
  add_json_library(iitdvpncli)
  target_compile_options(iitdvpncli PRIVATE -Wno-deprecated-declarations)
  target_compile_definitions(iitdvpncli PRIVATE -DOPENVPN_COMMAND_AGENT)
  return()
endif()
if (NOT WIN32)
  return()
endif()
add_executable(iitdvpncli openvpn.cpp)
add_core_dependencies(iitdvpncli)
add_json_library(iitdvpncli)
target_compile_definitions(iitdvpncli PRIVATE -DOPENVPN_COMMAND_AGENT -DOVPNAGENT_DISABLE_PATH_CHECK)

add_executable(omicli openvpn.cpp)
add_core_dependencies(omicli)

if (CLI_OVPNDCOWIN)
  target_compile_definitions(iitdvpncli PRIVATE ENABLE_OVPNDCOWIN)
  target_compile_definitions(omicli PRIVATE ENABLE_OVPNDCOWIN)
  target_link_libraries(iitdvpncli "bcrypt.lib")
  target_link_libraries(omicli "bcrypt.lib")

endif ()
